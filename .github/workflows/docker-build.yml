name: Build Docker Image

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Log in to Docker registry
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          registry: swr.tr-west-1.myhuaweicloud.com

      - name: Build Docker image
        working-directory: url-shortener
        run: docker build -t swr.tr-west-1.myhuaweicloud.com/exci_case/url-shortener:latest .
      
      - name: Push Docker image
        run: docker push swr.tr-west-1.myhuaweicloud.com/exci_case/url-shortener:latest
